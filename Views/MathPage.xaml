<Page
    x:Class="REToolBox.Views.MathPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:REToolBox.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- 顶部输入区域 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="10" Spacing="10">
            <!-- 函数输入框 -->
            <TextBox x:Name="FunctionInput" PlaceholderText="输入函数表达式 (例如: x*sin(x))" Width="300"/>
            <!-- 计算模式选择 -->
            <ComboBox x:Name="ComputeMode" SelectedIndex="0" Width="100">
                <ComboBoxItem Content="CPU"/>
                <ComboBoxItem Content="GPU"/>
            </ComboBox>
            <!-- 绘制按钮 -->
            <Button x:Name="DrawButton" Content="绘制" Click="DrawButton_Click"/>
            <!-- 清除按钮 -->
            <Button x:Name="ClearButton" Content="清除" Click="ClearButton_Click"/>
        </StackPanel>
        <!-- 绘图区域 -->
        <Grid Grid.Row="1" Background="White">
            <!-- 用于绘制函数图像的画布 -->
            <Canvas x:Name="GraphCanvas" Background="White" SizeChanged="GraphCanvas_SizeChanged"/>
            <!-- 状态文本提示 -->
            <TextBlock x:Name="StatusText" HorizontalAlignment="Center" VerticalAlignment="Center"
                      Text="请输入函数并点击绘制按钮" Foreground="Gray"/>
        </Grid>
    </Grid>
</Page>
